




<ul>
    <% gData.forEach(gText => { %>
    <li><%=gText.title; %></li>
    <% }) %>
</ul>





<% book.forEach(gText => { %>







// Show
gRouter.get('/show', (req, res) => {
	gBank.find({}, (error, allGs) => {
		res.render('show.ejs', {
			gData: allGs,
		});
	});
});